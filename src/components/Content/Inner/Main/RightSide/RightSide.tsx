import React from 'react';
import { Button } from '../../../../Button/Button';

export const RightSide: React.FC = () => {
	return (
		<section className="main-content__right">
			<Button text="Хочу исполнить мечту" />
		</section>
	);
};